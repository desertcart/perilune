source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in perilune.gemspec.
gemspec

gem 'sprockets-rails' # Required from Rails 7
gem 'identity', path: '../identity'
gem 'trifle-logger'
